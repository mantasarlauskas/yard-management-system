@model yard_management_system.Models.OrderContract
@{
    ViewData["Title"] = "Kontragento pridėjimas";
}

<h2>Pridėti krovinio kontragentą</h2>

<h4>Krovinys nr. @ViewData["ID"]</h4>
<hr />

@if ((int)ViewData["count"] > 0)
{
    <div class="row">
        <div class="col-md-4">
            <form asp-action="Create" asp-route-id="@ViewData["ID"]">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group">
                    <label asp-for="ContractorID" class="control-label">Laikas</label>
                    <select asp-for="ContractorID" class="form-control" asp-items="ViewBag.ContractorID"></select>
                    <span asp-validation-for="ContractorID" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <input type="submit" value="Išsaugoti" class="btn btn-default" />
                </div>
            </form>
        </div>
    </div>
}
else
{
    <div class="alert alert-danger">Kontragentų, kuriuos butų galima priskirti kroviniui, šiuo metu nėra</div>
}

<div>
    <a asp-controller="Cargoes" asp-action="Details" asp-route-id="@ViewData["ID"]">Grįžti į krovinio informaciją</a>
</div>

